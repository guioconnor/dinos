const prefix = 'dinos';

const initialState = {};

// reducer
const dinosReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default dinosReducer;

const initialState = {
  foo: 'bar',
};

// reducer
const memoryBoardReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default memoryBoardReducer;
